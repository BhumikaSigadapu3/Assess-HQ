{
  "name": "assess-hq",
  "version": "1.0.0",
  "private": true,
  "description": "Assess HQ — AI examination and hiring platform",
  "scripts": {
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:seed": "docker compose exec backend npm run seed:admin"
  }
}
